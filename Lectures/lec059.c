#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/* Strings are stored as arrarys, no string type only char type */
/* The underlying array must be large enough to store the full string with 1 additional byte */
